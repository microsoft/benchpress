# Score Matrix Figures (§3.2)

## Paper mapping
- **Section**: §3.2 The Benchmark Score Matrix
- **Composite figure**: `fig:data_distribution` (main_body.tex)
- 5 panels (all generated by this dir's `plot.py`):
  - `figures/bp_releases.pdf` — model releases over time
  - `figures/bp_coverage.pdf` — coverage by release quarter
  - `figures/bp_bench_mix.pdf` — benchmark mix by category
  - `figures/bp_obs_concentrate.pdf` — observed cells by category
  - `figures/bp_source_provenance.pdf` — source provenance pie (`fig:source_provenance`)

## How to run
```bash
cd ~/Documents/submission/benchpress/github
python experiments/sec3_low_rank/score_matrix/plot.py
```

Lightweight, deterministic. Overwrites all 5 figures + their `.png` companions.

## Inputs
- `benchpress.build_benchmark_matrix.load_score_matrix` (panels 1–4)
- Raw audit records via `benchpress.build_benchmark_matrix.build_benchmark_matrix._load_raw` + canonical rules (panel 5 only)

## Last valid result
- Matrix: 84×133 (2,604 observed, 23.3% fill).
- Source provenance breakdown: ~80% model provider, remainder split between leaderboard and third-party.
